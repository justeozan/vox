## What & why

<!-- What does this change, and why? Link any related issue (Fixes #123). -->

## How I tested

<!-- Vox is a voice app: describe how you verified it on a real signed bundle
     (scripts/dev-bundle.sh), not just that it compiles. -->

## Checklist

- [ ] Builds (`cd src-tauri && cargo build`)
- [ ] Tested on a real signed bundle (`scripts/dev-bundle.sh`), mic path included if relevant
- [ ] Stays local-first (no cloud calls, no API keys, no telemetry)
- [ ] Updated docs/README if behavior or config changed
